#include<iostream>
using namespace std;

// THink of templates as
/*"Write the algorithm once.

Let the compiler make versions for every datatype."*/

/*
Syntax for template
Template <class T>  (T can be any data type)
class vector{
T* arr;
Public:
    vector(T* arr){
    code 
    }
    
    
};

*/
int main(){
    // vector <int> myvect(ptr);
    // vector<float>myfvec(ptr);
    return 0;
}