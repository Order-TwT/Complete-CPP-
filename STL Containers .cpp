/*
STL --> Standard Template Library 
Components of STL ->
1. Containers -> stores data using template classes
2. Algorithm  -> use template fucntions 
              -> set of procedures like sorting and searching
3. Iterators  -> object which points to an element in the container
              -> handled just like pointers
              -> connects algorithms with containers
*/
/*
CONTAINERS: 
1. Sequence containers --> Store data in Linear fashion 
                       --> Vector ---> Random access fast, insertion/deletion is slow
                                       but insertion at end is fast
                       --> list --> RA is slow, insertion in middle is faster
                                    insert/delete at end is also fast
                       --> Dequeue

2. Associative --> Direct Access 
               --> Trees-->set,multiset,map/multimap
               --> Other than Random access all operations are fast
3. Derived --> Real world modelling
           --> Stack, Queue, priority queue
           --> Depends 
*/
#include<iostream>
using namespace std;

int main(){
    
    return 0;
}